name: signet
description: "Cryptographic multi-factor verification for human relationships. Defends against voice/video deepfake vishing via device-to-device TOTP codes. Zero server, offline-first."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.3.4+30004

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.1
  cryptography: ^2.9.0
  cryptography_flutter: ^2.3.4
  flutter_secure_storage: ^10.0.0
  # FOSS QR scanner: ZXing C++ via FFI. Replaced mobile_scanner in v0.3.1
  # because mobile_scanner bundles Google ML Kit + Play Services as native
  # code (proprietary), which blocks F-Droid inclusion (Inclusion Policy
  # item 5). flutter_zxing is MIT, no GMS deps.
  flutter_zxing: ^2.3.0
  # Direct dep on `camera` so we can name `CameraException` in the
  # permission-denial fallback. flutter_zxing pulls camera in transitively
  # but its `export` clause omits CameraException specifically.
  camera: ^0.12.0
  qr_flutter: ^4.1.0
  bip39: ^1.0.6
  # Non-sensitive app prefs (onboarding-completed flag etc.). Secrets
  # live in flutter_secure_storage via SecureStore; this is for flags.
  shared_preferences: ^2.5.3
  # PDF generation + platform print dialogs for the challenge-response
  # paper card export. Both packages are pure-Dart/offline; print
  # dialogs render via the OS, no network.
  pdf: ^3.11.3
  printing: ^5.13.4
  # Platform share sheet for exporting backup packages + PAKE to a
  # destination of the user's choice (Files / Drive / Notes / etc.).
  # Share sheet is OS-mediated; we don't make network calls.
  # Pinned at ^12 (not 13) because 13.x requires win32 ^6.0.1 which
  # conflicts with flutter_secure_storage's transitive
  # flutter_secure_storage_windows ^4.1.0 (locked to win32 ^5.5.4).
  # Revisit after flutter_secure_storage_windows bumps its win32 dep.
  share_plus: ^12.0.2
  # File picker for importing a saved backup. User opens a picker,
  # chooses a .txt file, we read it locally. No network.
  file_picker: ^11.0.2
  # Launches external URLs from the About screen (source, privacy,
  # support/donate). Hands off to the OS browser; no network calls
  # from Signet itself.
  url_launcher: ^6.3.1
  path_provider: ^2.1.5
  package_info_plus: ^9.0.1
  # Pinned at ^11.x: 12.x uses `NSProcessInfo.isiOSAppOnVision`
  # (iOS 17 / Xcode 15 SDK), which the macos-latest GitHub runner's
  # default Xcode doesn't yet ship. v11.x supplies the same fields
  # we consume (manufacturer/model/version) and builds clean on the
  # runner. Bump back to ^12 once macos-latest defaults to Xcode 16+.
  device_info_plus: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5
  # Generates the full iOS + Android icon asset set from a single source
  # image. Config lives in flutter_launcher_icons.yaml alongside this
  # file. Run: `dart run flutter_launcher_icons`.
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
